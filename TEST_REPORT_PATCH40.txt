ANIME BATTLE MULTIVERSE — PATCH40 TEST REPORT
Дата сборки: 2026-07-13
Версия: PATCH40_FINAL_MULTIVERSE
schema_version: 40
storage_version: 40.0

1. ИСХОДНЫЕ АРХИВЫ
- Безопасная основа: PATCH37 / прототип 36 №1.
- PATCH35 использован как compatibility manifest/reference.
- Фактический «прототип 36 №2» распакован и изучен. Перенесены custom emoji и UI/season идеи. Его упрощённое/небезопасное хранение, fallback при Neon outage, права и рискованные изменения пула не переносились.

2. СТАТИЧЕСКИЕ ПРОВЕРКИ — ПРОШЛИ
- Python py_compile: bot.py и все PATCH40 tests.
- pyflakes: 0 ошибок/предупреждений по bot.py и тестам.
- ast/symtable undefined global audit: 0 возможных undefined names.
- duplicate top-level definitions: 0.
- static callback_data без handler: 0.
- callback handlers без callback.answer: только 3 заранее аудированных делегата; их helper-функции вызывают answer.
- callback_data >64 bytes: 0.
- drop_pending_updates=False присутствует; drop_pending_updates=True отсутствует.
- PerUserSerialMiddleware, global error handler, pity и global payment ledger присутствуют.
- TODO/FIXME/NotImplementedError в bot.py отсутствуют.

Текущее покрытие интерфейса:
- exact callback patterns: 92;
- dynamic callback prefixes: 79;
- commands: 51.

PATCH35 compatibility manifest полностью сохранён:
- старые exact callbacks;
- старые dynamic prefixes;
- старые commands.

3. КАРТЫ И МЕДИА — ПРОШЛИ
- cards.json: 17 641 запись.
- непустых уникальных ID: 17 641.
- дубликатов ID: 0.
- исходные card IDs не менялись.
- реальные media members: 896.
- уникальные распознанные media IDs: 896.
- orphan media IDs: 0.
- unsafe ZIP paths: 0.
- zipfile.testzip для обоих media_packs: успешно.

SHA-256:
- cards.json: 20e33de2ba872d37a627801df26835b5a8e7a08f7f625285cc0a4d0e4232f1e4
- card_images_bundle_1.zip: f4129ef48a7dbb7ba4682aa935e73be6f8f38dd333de6cf35ae0c215dbca4240
- card_images_bundle_2.zip: 86bd05fed5695567cd893379fb5afbd2e98f24796482ce07b9c59772f07978cc

4. CORE SIMULATION — 58/58 ПРОШЛИ
Проверены:
- 17 641 карт и 896 media index;
- новый аккаунт: 1500 фисташек до onboarding, 0 попыток и пустая коллекция;
- onboarding выдаёт ровно 5 карт, 3 попытки и 100 SP один раз;
- прерывание onboarding переживает restart;
- legacy-аккаунт не получает onboarding и сохраняет баланс, карту, Premium, клан, бан и заморозку;
- permanent tombstone блокирует recreation;
- technical purge разрешает безопасный новый старт;
- recovery добавляет только отсутствующего разрешённого игрока, не меняет существующего и не возвращает tombstones;
- реферал: точные +300/+1 и +500/+3, повторы и другой inviter заблокированы;
- промокоды: atomic max_uses, repeat protection, card/shards reward, owner test;
- parallel daily claim выдаёт награду один раз;
- season 28-day boundaries, idempotent SP key, claim once, rating filters;
- одинаковый пользователь сериализуется, разные пользователи работают параллельно, idle locks очищаются;
- high-risk duplicate callback debounce;
- snapshot mutation сохраняет следующую revision;
- failed save оставляет dirty и повторяется;
- configured Neon без драйвера/доступа fail-closed;
- payment first grant, global duplicate rejection, parallel duplicate successful_payment exactly once;
- strict pre_checkout;
- rarity/pity сохраняются;
- concurrent generated-card cache, lock cleanup, GIF/MP4/photo routes;
- no-Pillow fallback, simulated disk-full fallback и hard cache limit.

5. PATCH40 FEATURE SIMULATION — 28/28 ПРОШЛИ
Проверены:
- custom button emoji включены по умолчанию и не дублируются обычным emoji в тексте кнопки;
- семь главных разделов меню;
- первый экземпляр — полный персонаж;
- дубликат остаётся одним персонажем и добавляет фрагменты;
- fragment reward открывает полного персонажа;
- legacy fragment-only запись мигрирует без потери фрагментов;
- lore-description не содержит PATCH/collection/draw/deck boilerplate;
- сезонная карта: первый раз полный персонаж, второй раз фрагменты;
- единый collection numerator/denominator;
- main stats внутри blockquote;
- нет «ГОТОВ» в кнопке;
- public profile очищен;
- right hand отключена по умолчанию;
- artifact detail/equip и сохранение общего team bonus;
- events имеют только daily, raid-hit и menu; tournament automatic;
- global reset исключает владельца, очищает gameplay, сохраняет payments/Premium и recent activity, удаляет right-hand identity;
- inactive account auto-purge;
- preserved paid state возвращается после technical purge;
- artifact callback_data ≤64 bytes.

6. RUNTIME UI SMOKE — 14/14 ЭКРАНОВ ПРОШЛИ
Проверены с реальными установленными aiogram-моделями:
- main;
- collection home;
- rarity collection;
- collection list;
- artifacts;
- artifact detail;
- profile;
- profile statistics;
- modes;
- events;
- rewards;
- more;
- season;
- admin.

Для каждого проверено:
- сообщение/caption создано;
- message ≤4096, media caption ≤1024;
- HTML-теги b/i/code/blockquote/tg-emoji сбалансированы;
- все callback_data ≤64 bytes;
- главное меню содержит «Призвать» без «ГОТОВ»;
- смена вселенной отсутствует в коллекции и присутствует в профиле;
- события имеют ровно одну boss action и не имеют отдельной tournament button;
- artifact detail показывает описание и бонус.

7. РЕАЛЬНЫЙ RUNTIME IMPORT — ПРОШЁЛ С ОГРАНИЧЕНИЕМ ВЕРСИИ PYTHON
Среда:
- Python 3.13.5;
- aiogram 3.27.0;
- aiohttp 3.13.5;
- Pillow 10.4.0;
- psycopg 3.3.4 + psycopg-binary 3.3.4.

С чистым временным DATA_DIR и exact requirements успешно импортированы:
- PATCH40_FINAL_MULTIVERSE;
- 17 641 cards;
- 896 real media IDs;
- schema_version 40.

Python 3.12 в среде отсутствовал. Файл .python-version закрепляет 3.12, но реальный import именно на Python 3.12 не выполнялся и не заявляется как выполненный.

8. НЕВОЗМОЖНЫЕ В ЭТОЙ СРЕДЕ PRODUCTION E2E-ТЕСТЫ
Не выполнялись и не выдумываются:
- реальное подключение/отключение production Neon;
- реальная запись/rollback production DATA;
- реальная Telegram Stars покупка, refund и повторная delivery от Telegram;
- настоящий Render deploy и health port в Render;
- фактический reset живых пользователей в Neon.

Вместо этого storage/payment/concurrency ветки проверены статически и изолированными симуляциями. Для live reset после backup предусмотрена owner-only команда /reset_patch40 CONFIRM.

9. УПАКОВОЧНАЯ ГИГИЕНА
Перед упаковкой удаляются:
- __pycache__;
- .pyc;
- runtime JSON/SQLite/logs;
- extracted media/cards;
- generated cache;
- временные PNG;
- token.txt;
- секреты.

В архив включаются только bot.py, cards.json, media_packs, PATCH40 UI assets, конфигурационные шаблоны, документация и тесты.
Финальный ZIP отдельно проверяется zipfile.testzip, повторным secret scan и перечнем запрещённых файлов.
