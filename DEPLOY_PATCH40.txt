ANIME BATTLE MULTIVERSE — DEPLOY PATCH40

1. ДО ДЕПЛОЯ
- Создай Neon branch/snapshot или pg_dump.
- Сохрани предыдущий рабочий commit/ZIP.
- Убедись, что не запущены два экземпляра одного bot token.
- Проверь Render Environment: BOT_TOKEN, DATABASE_URL, OWNER_ID или OWNER_IDS.
- RIGHT_HAND_ID/RIGHT_HAND_IDS можно оставить пустыми. Даже старые значения не дают прав без ABM_ENABLE_RIGHT_HAND_PERMISSIONS=1.

2. ЗАГРУЗКА
- Замени код файлами PATCH40.
- Не загружай runtime JSON/SQLite, токены и DATABASE_URL.
- Оставь .python-version=3.12, requirements.txt и Procfile без изменений.
- Выполни Clear build cache & deploy.

3. ПЕРВАЯ ПРОВЕРКА
От владельца:
/storage
/flush_data
/menu
/season

Затем вручную открой:
- Призвать;
- Коллекцию и артефакты;
- Битвы;
- События;
- Награды;
- Профиль;
- Админ-панель → Хранилище и Оплаты pass.

4. ЗАПРОШЕННЫЙ СБРОС ИГРОКОВ
PATCH40 намеренно не сбрасывает live Neon автоматически.
После backup и успешной проверки /storage выполни ровно один раз:
/reset_patch40 CONFIRM

Сбросит всех кроме владельца. Сохранит global payment ledger, подтверждённые покупки и действующий оплаченный Premium/MultiPass.
После команды снова выполни /flush_data и проверь подтверждение Neon.

5. ЕСЛИ NEON НЕДОСТУПЕН
- Не включай локальный fallback.
- Бот должен остановиться до polling — это защита.
- Проверь DATABASE_URL, сеть Neon и psycopg.
- После восстановления Neon перезапусти deploy.

6. ОТКАТ
- Останови PATCH40.
- Сделай backup текущего Neon.
- Верни предыдущий код с той же DATABASE_URL.
- Не подменяй Neon старым JSON/SQLite.
- При необходимости восстанови заранее созданную Neon branch/snapshot.
